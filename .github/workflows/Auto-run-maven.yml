name: Trigger Another Workflow

on:
  workflow_dispatch:

jobs:
  trigger_workflow:
    uses: actions/workflow-dispatch@v1
    with:
      ref: main  # Replace with the branch to trigger the workflow on
      workflow_id: MyOtherWorkflow 
